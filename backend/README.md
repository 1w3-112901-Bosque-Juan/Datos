Backend quick start:

- cd backend
- mvn spring-boot:run

Before running, make sure Redis is running locally (default port 6379) and application.properties contains a valid MongoDB Atlas URI.

Use the provided Redis and MongoDB initialization commands (in project root README) to insert test data.
